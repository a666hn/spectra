syntax = "proto3";

package customer;

service Customer {
    rpc FetchCustomer(RequestCustomer) returns (Response) {}
}

message RequestCustomer {
    string customerID = 1;
}

message CustomerData {
    string ID = 1;
    string fullName = 2;
    string address = 3;
    bool status = 4;
}